# Recorded responses

No NPS responses have been recorded yet. This file verifies the resource bundle layout.

Record real responses when implementing an operation. Use the application identity
`(swift-nps, https://github.com/KalebCooper/swift-nps)` and supply credentials outside source control.
Document each exact path and query in the corresponding `Fixture` case. Never commit API keys.
Preserve nulls, unknown values, identifiers, and the original response envelope.
